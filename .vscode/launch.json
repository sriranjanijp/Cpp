{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run C++ (No Debugging)",
            "type": "cppdbg",
            "request": "launch",
            "program": "${fileDirname}\\${fileBasenameNoExtension}.exe",
            "cwd": "${fileDirname}",
            "externalConsole": true,
            "MIMode": "gdb",
            "stopAtEntry": false
        }
    ]
}
